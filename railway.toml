[build]
builder = "nixpacks"

[deploy]
startCommand = "until python manage.py migrate; do echo 'DB not ready, retrying in 3s...'; sleep 3; done && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/health/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
